案例目录模板
==============

把下面几类文件放在同一个文件夹里。文件名可以任意。

需要：
- JDBC / 应用异常日志（必须能看出网络类异常）
- 应用侧 PCAP
- 数据库侧 PCAP

可选：
- Ping 日志

然后在 DBCAP 目录执行：

  run.bat auto-case "这个文件夹的完整路径" --port 数据库端口 --server-ip 数据库IP

如果程序说角色不明确，按提示加上 --app-pcap 和 --db-pcap。
不要把生产数据打进发布包。本目录只是说明，不含样例抓包。
